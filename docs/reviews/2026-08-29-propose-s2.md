---
role: proposer
date: 2026-08-29
agent: canfar-066-s2
canon_generation: 4
ids:
  - DEC-066-INFER
  - DEC-066-SPECRESP
  - DEC-066-WEIGHT
  - DEC-066-POL
  - DEC-066-SHIFT
  - DEC-066-ZEROMODEL
  - DEC-066-REPO
  - DEC-066-TARGET
verdict: propose
---

# S2 hybrid: Laplace-MH plus SBC, not autodiff NUTS

**Read after** `AGENTS.md` -> field-guide -> `STATUS.md` -> S1 note `docs/diagnostics/s1-mock.md`. Canon generation 4. No new `DEC-*` id. User licensed the hybrid path. Official MAP remains `kinuv-KGAS066-uvsign-map`. Stage A only.

ASCII only in this file: `chi2`, `gas_sigma`, `r_t`, `R_hat`, `ESS`.

## Scope

Existing ids. INFER gates are met (MAP `Delta_chi2` vs V=0 = +35553; S1 mock recovered `V_0`, `gas_sigma`, `r_t` on Hann+bin). User asked for S2 coverage and a real-data variance scale. Sampler is **not** NumPyro NUTS.

## What changed / what was checked

- No JAX `predict_binned`. Production Jacobian is finite difference (~0.45 s/eval). FD HMC is tens of CPU-hours per chain.
- [DEC-066-REPO](../decisions/DEC-066-REPO.md): do not add `emcee` as a required dep.
- User chose hybrid: one Stage A MCMC on the S1 inject (`sampler: laplace_mh`) plus ~20 Laplace SBCs; real-066 CI table with temperature `T`.
- `chi2 = s * sum w |d-m|^2`. `E[chi2] = 2 * n_vis` for a correct model (`n_vis = n_row * n_chan`). Official `chi2 / n_vis ≈ 2.016` is **not** a factor-of-two noise bug; `chi2_red = chi2 / (2 n_vis) ≈ 1.008`. Product scale is `T_dof`. Sensitivity column is `T_nvis = chi2 / n_vis`. Mocks use `T = 1`.

## Rejected alternatives

- Autodiff NUTS this wave — no JAX likelihood.
- Calling FD Metropolis "NUTS" in stdout or JSON.
- Inflating mock coverage by `chi2/n_vis`.
- Stage B `lambda=0` sampling.
- New `DEC-*`.

## Residual risks

1. `laplace_mh` can fail `R_hat` / `ESS` if the Hessian is a poor proposal. Then stop and report eval/s; do not switch to dynesty.
2. SBC N=20 has a wide binomial interval; pass is "consistent with 68/95%", not a point match.
3. `T_dof ≈ 1` does not fix structured leftover vs velocity (SB misspecification). Real-galaxy intervals can still be over-confident after scaling.
4. XX-only `s` still leaves `sqrt(2)` vs XX+YY ([DEC-066-POL](../decisions/DEC-066-POL.md)).
5. Y1 (`1e5` evals in minutes) remains a fail on CPU.

## STATUS updates required

- `next_role: reviewer` after artifacts land
- `pending: []`
- `last_propose:` this file
- Official product unchanged: `kinuv-KGAS066-uvsign-map`
- `deadlocks:` none
