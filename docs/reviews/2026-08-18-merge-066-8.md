---
role: proposer
date: 2026-08-18
agent: chat-A
canon_generation: 4
ids:
  - DEC-066-INFER
  - DEC-066-ZEROMODEL
  - DEC-066-VC
  - DEC-066-SHIFT
verdict: propose
---

# 066-8 merge note: Δχ² beats V=0; Stage A vector is not a kinematics MAP

No new DEC. Plumbing from [dispatch 066-8](2026-08-18-dispatch-066-8.md) is on `feat/066-8-map` `7f9f125`.

## What was checked

- Model path is `predict_vis` on `freqs_native` → `kinuv.response.spectral.hann_then_bin` → `chi2` / `chi2_zero`. Not `native_diagonal`. Not `from kinuv.likelihood import hann_then_bin`.
- `(dx, dy)` seed `(0, 0)`, box ±2″, prior σ=0.5″ on the objective only. Gate is likelihood Δχ².
- No vis phase ramp in `infer/map.py`. Frozen `i`. No rings. `infer/map.py` is 375 lines.
- Fit array matches 066-6: 881 × 95, N=4, Δv=5.080 km/s, s=0.514.
- One eval **0.708 s** (modest miss of the ~0.5 s GRID note; not a CANFAR trigger).
- Gate 3 number: χ²_MAP=1.999×10⁵, χ²_zero=2.042×10⁵, **Δχ²=+4341**. MAP beats V=0. Do not NUTS from a loss. Do not NUTS from this vector either.

## Attack (not a rubber stamp)

Δχ² > 0 is necessary for DEC-066-INFER and is **not** sufficient kinematics. The reported Stage A point is a high-flux non-rotating blob on the dispatch box:

| parameter | value | box |
|---|---|---|
| flux | 47.29 Jy | interior |
| PA | 175.200° | **seed−30° wall** |
| vsys | 8228.95 km/s | interior |
| gas σ | 50 km/s | **upper wall** |
| dx, dy | 1.467″, 2.000″ | **dy at +2″** |
| V_0, r_t | 0 km/s, 0.500″ | **both floors** |

`V_0 = 0` makes PA and `r_t` degenerate. The dispatch PA ±30° was an implementation box, not an ADR. Hitting it with `V_0` at the floor means the Δχ² is a **detection of a shifted Gaussian disk**, not a recovered arctan curve. Gate 4 must not initialise rings from this vector. Do not freeze `(dx, dy)` from it. Do not start 066-9/10/11.

## Residual risks

1. Bound-hitting Stage A (this file). Diagnosis next, not a new DEC until someone wants a `V_0` prior or a different PA box.
2. Eval 0.708 s vs ~0.5 s GRID note.
3. XX-only √2 until 066-9.
4. 066-7 mocks still use `native_diagonal` if they import `hann_then_bin` from `likelihood`.

## STATUS updates required

- Record Δχ²=+4341 and the bound-hitting vector
- `next_role: proposer`
- No NUTS. No rings on real vis. No new DEC.
