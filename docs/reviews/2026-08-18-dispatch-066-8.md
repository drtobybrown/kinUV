---
role: proposer
date: 2026-08-18
agent: chat-A
canon_generation: 4
ids:
  - DEC-066-INFER
  - DEC-066-ZEROMODEL
  - DEC-066-SHIFT
  - DEC-066-SPECRESP
  - DEC-066-VC
  - DEC-066-WEIGHT
  - DEC-066-VIS
verdict: propose
---

# Dispatch 066-8: Stage A L-BFGS MAP on real Hann+bin 066

User asked the new chat to pick up `docs/reviews/2026-08-18-handoff-new-chat.md`. Handshake: `next_role: proposer`, `code_freeze: false`. `dev` is at `554bee1` (Wave 1 + 066-6 `1017602` + 066-7 `8b06c2b`). No new DEC.

## Closed path

1. **Data:** `load_kgas066` on local `/Users/thbrown/kilogas/DR1/visibilities/KILOGAS066.npz`. Fit array already recorded: `n_row=881`, `n_chan=95`, `Δv=5.080 km/s`, `N=4`, `s=0.514`. Do not re-trim with YAML `obs_freq_range`. Do not Hann data.
2. **Model:** `predict_vis` on `VisData.freqs_native` (trim + guards) → `hann_then_bin` from `kinuv.response.spectral` (not `kinuv.likelihood`; 066-7’s import of `hann_then_bin` from likelihood is a miss — that is why mocks used `native_diagonal`). Weights for the bin operator are `VisData.weights_native` on the **trimmed** native axis. Then `chi2` / `chi2_zero` / `delta_chi2` with fit-array `s`.
3. **Shift:** Fourier image shift then `A` at the phase centre. No visibility phase ramp after PB. `(dx, dy)` are free: Gaussian prior σ=0.5″, box ±2″ (DEC-066-SHIFT). YAML `[0,0]` is the seed.
4. **Stage A vector (DEC-066-VC wins the handoff’s shorter list):** flux, PA (seed 205.2°), vsys (seed 8299.563 km/s), gas σ (seed 10 km/s), `(dx, dy)`, `V_0` (seed 200 km/s), `r_t` (seed 3″). Frozen `i=43.9°`. Handoff omitted `V_0`, `r_t`; they are Stage A parameters in the ADR.
5. **Objective:** L-BFGS-B on `χ² + (dx/0.5″)² + (dy/0.5″)²`. Gate is **likelihood** `Δχ² = χ²_zero − χ²_MAP` (prior not in the gate). Reduced χ² is not the gate. If `Δχ² ≤ 0`, stop; do not dynesty/NUTS (DEC-066-INFER / ZEROMODEL).
6. **Stage B rings:** **not this commit.** `λ_reg` is uncalibrated (`run_lambda_reg_campaign` still raises). DEC-066-VC: calibrate `λ_reg` **before** real visibilities. Gate 4 / 20×5 is after Stage A beats zero. Handoff “then rings if AIC” is the later product.

Hardware: this M1, jax-finufft. Time one aggregated eval (`predict_vis` + `hann_then_bin` + `chi2`). If it is tens of seconds, stop and report; a modest miss of the ~0.5 s GRID note is not a CANFAR trigger (066-11 is later). Do not import uvkin/KinMS.

## Rejected

- Native-window `diagonal_chi2` as the MAP product (066-7 stand-in only).
- Fitting 1920 native channels.
- Freezing `(dx, dy)` at 0.
- Vis phase ramp after PB.
- Uncalibrated rings on real vis.
- NUTS / dynesty if MAP loses to V=0.
- Growing `forward/mocks.py` or `io/vis.py` past 400 lines — new `kinuv.infer`.

## Residual risks

1. One aggregated eval may exceed ~0.5 s (`freqs_native` is wider than the 066-7 99-channel mock window).
2. Finite-difference Jacobian (066-7 style) multiplies evals; MAP may take tens of minutes.
3. XX-only √2 remains until 066-9.
4. `s=0.514` is already on the fit array; do not replace with YAML 0.5 or `12/29`.

## STATUS updates required

- `phase: 066-8`
- `last_propose:` this file
- `next_role: proposer` (implementation dispatch, no new DEC)
- worker: isolated `feat/066-8-map`, commit `feat(066-8): …`
