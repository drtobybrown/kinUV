---
role: proposer
date: 2026-08-18
agent: chat-A
canon_generation: 2
ids:
  - DEC-066-SPECRESP
  - DEC-066-SB
  - DEC-066-PB
  - DEC-066-OSCMETRIC
  - DEC-OPS-AUTH
verdict: propose
---

# Hello / generation-2 dump (provisional)

Chat A landed generation-2 ADR patches after a pre-build review. There was **no** second-agent ACK. Chat B is the reviewer (`STATUS.next_role: reviewer`).

## What landed (do not re-propose; review)

- SPECRESP: Hann on native MS grid then bin; empirical `s` only; `12/29` is a unit test; ρ=5.2%.
- SB: Wiener `K=(σ/I_peak)²`; Jy at observed ν; deleted “multiply vis by B̃”.
- PB: mandatory (`pbcor=True`); `A(x,y)` before NUFFT; FWHM=`1.13 λ/D`≈25.9″; ~21% at 7.5″.
- OSCMETRIC: spline second-difference prior, not SE-GP; 20 mocks × 5 λ; `Δv` from npz.
- OPS-AUTH: `kinuv-KGAS066-{sha6}-{map|nuts}`; cert at launch; campaign cron deferred.

## Rejected in that pass

Applying `12/29` and `s≈0.826` together. Rest-frequency Jy/K. `56.6″/ν_GHz` PB formula. Session names `KILOGAS066[:8]`. PLAN.md Q9 “PB deferred for 066”.

## Residual risks (reviewer must add at least one more, or challenge)

1. 066 npz `N` and `Δv` are still unrecorded (replica bin-4 vs YAML bin-8).
2. Whether CASA `WEIGHT` on the actual npz already includes `EFFECTIVE_BW` is inferred from ⟨w|V|²⟩=2.42, not measured on the file that will be fit.
3. Ico is restored+pbcor at 30 km/s; Wiener of residuals may still ring at the 1709-pixel mask.

## STATUS

`next_role: reviewer`. `code_freeze: true`. Clear `pending` only after a valid review file.
