# Campaign configuration

A campaign configuration resolves the modeling and execution choices reviewed for one scientific question. Copy `_template.yaml`, replace placeholders, and freeze its SHA-256 before production data are fit.

Quantitative gates are campaign inputs established by the Consultant before production answers are inspected. A changed model, prior, covariance, data selection, transform convention, or gate threshold creates a new revision and repeats independent review.
